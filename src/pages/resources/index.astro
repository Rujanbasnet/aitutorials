---
import BaseLayout from '@layouts/BaseLayout.astro';
import PostCard from '@components/content/PostCard.astro';
import { getPublishedResources } from '@utils/collections';

const resources = await getPublishedResources();
---

<BaseLayout title="Resources" description="Cheat sheets, reference guides, tool directories, and templates to accelerate your AI workflow.">
  <section class="relative overflow-hidden bg-warm">
    <img
      src="https://images.unsplash.com/photo-1507842217343-583bb7270b66?w=1600&h=400&fit=crop&q=80"
      alt=""
      class="absolute inset-0 w-full h-full object-cover opacity-[0.06]"
      loading="eager"
      aria-hidden="true"
    />
    <div class="relative max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-16 lg:py-20">
      <p class="label mb-3">Tools & references</p>
      <h1 class="text-title mb-3">Resources</h1>
      <p class="text-lg text-[var(--color-text-secondary)] max-w-xl">
        Cheat sheets, references, and tools to accelerate your AI workflow.
      </p>
    </div>
  </section>

  <section class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    {resources.length > 0 ? (
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 stagger">
        {resources.map((resource) => (
          <div class="fade-in">
            <PostCard
              title={resource.data.title}
              description={resource.data.description}
              href={`/resources/${resource.id}/`}
              pubDate={resource.data.pubDate}
              tags={resource.data.tags}
              image={resource.data.image}
              body={resource.body || ''}
            />
          </div>
        ))}
      </div>
    ) : (
      <div class="card-warm p-16 text-center rounded-xl">
        <h3 class="text-heading mb-2">Coming soon</h3>
        <p class="text-[var(--color-text-secondary)]">Resources are being prepared. Check back soon.</p>
      </div>
    )}
  </section>
</BaseLayout>
