---
interface Props {
  href?: string;
  class?: string;
  padding?: string;
}

const { href, class: className = '', padding = 'p-6' } = Astro.props;
const Tag = href ? 'a' : 'div';
---

<Tag
  href={href}
  class:list={['glass-card block', padding, className]}
>
  <slot />
</Tag>
